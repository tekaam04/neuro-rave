services:
  neuro-rave:
    build: .
    ports:
      - "8050:8050"
    environment:
      - BIOSEMI_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./src:/app/src
      - ./main.py:/app/main.py
    restart: unless-stopped